   //Concatenção de elementos de dois (ou mais) Arrays

let frutas = ['maçã', 'banana'];

let maisFrutas = ['laranja', 'pera'];

let concatenção= frutas.concat(maisFrutas);

 console.log(concatenção);